syntax = "proto3";

package user;

message User {
    int32 id = 1;
    string email = 2;
    string password = 3; // Ensure to handle passwords securely
}

service UserService {
    rpc CreateUser(User) returns (User);
    rpc FindByEmail(User) returns (User);
}
